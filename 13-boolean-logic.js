/**
 * Basic logical operators => AND, OR NOT operator
 */

/**
 * AND -> returns true only id A and B are both true
 * We can join more AND but only if all are true that it evaluates to true
 */

/**
 * OR -> It will be true if only one of the variable is true
 *
 */

let isVerified = false;

if (!isVerified) {
  console.log('Congrats');
}

/**
 * An expression is a piece of code that produces a value
 * e.g 3.4 is an expression bkox it will produce a value
 */

/**
 * A statement is a bigger piece of code that is executed but does not produce a value
 * They are more like full sentences
 * if (23 > 10) {
 *  const str = 'why
 * }
 */

/**
 * Expressions and statements are important bkox in template literals we can only insert expressions and not statements
 * This analogy is also true when working with react
 * e.g const me = `I am ${2022-2021} years old`;
 */

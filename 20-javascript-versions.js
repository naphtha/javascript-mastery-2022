/**
 * How javascript versions work
 * ES6 is same as ECMASCRIPT 2015
 * JavaScript ensures backward compatibility -> Don't break the web
 */

/**
 * During production use Babel to transpile and polyfill our code (converting back to ES5 to ensure browser compatibility)
 */

/**
 * ES6 thru ES2020 can all be shortened and called ES6+
 * Future releases are called ESNext
 */
